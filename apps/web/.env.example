# Local development — copy to apps/web/.env (gitignored).
PUBLIC_GAME_SERVER_URL=http://localhost:8787

# Production: set PUBLIC_GAME_SERVER_URL in the Vercel dashboard.
# Do not commit a .env.production file.
