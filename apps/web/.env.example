# URL of the deployed Cloudflare Worker (game server).
# Local:
PUBLIC_GAME_SERVER_URL=http://localhost:8787

# Production (Vercel → Environment Variables → Production + Preview):
# PUBLIC_GAME_SERVER_URL=https://chowka-bhara.siddwayy-dev.workers.dev
