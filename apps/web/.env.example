# Local development (copy to apps/web/.env — gitignored).
PUBLIC_GAME_SERVER_URL=http://localhost:8787

# Production/preview builds use apps/web/.env.production (committed).
# Optional: override on Vercel → Environment Variables for Production + Preview.
