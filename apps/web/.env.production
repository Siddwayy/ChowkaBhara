# Baked into production/preview builds (not a secret — clients call this URL).
# Vercel dashboard env with the same name still overrides this if set.
PUBLIC_GAME_SERVER_URL=https://chowka-bhara.siddwayy-dev.workers.dev
